import { Card, CardContent, CardDescription, CardHeader, CardTitle } from '@/components/ui/card'
import { Table, TableBody, TableCell, TableHead, TableHeader, TableRow } from '@/components/ui/table'
import type { FinanceProjectSummaryDto } from '@/types/finance'
import { formatMoney } from '../financeUi'

export function FinanceProjectTable({ projects }: { projects: FinanceProjectSummaryDto[] }) {
  return <Card><CardHeader><CardTitle className="text-base">Projektkosten und Marge</CardTitle><CardDescription>Soll-Umsatz aus Leistungen; Ist-Umsatz ausschließlich aus gebuchten Einnahmen</CardDescription></CardHeader><CardContent><div className="rounded-lg border"><Table><TableHeader><TableRow><TableHead>Projekt</TableHead><TableHead className="text-right">Soll-Umsatz</TableHead><TableHead className="text-right">Ist-Umsatz</TableHead><TableHead className="text-right">Personal</TableHead><TableHead className="text-right">Subunternehmen</TableHead><TableHead className="text-right">Weitere Kosten</TableHead><TableHead className="text-right">Ergebnis</TableHead><TableHead className="text-right">Marge</TableHead></TableRow></TableHeader><TableBody>{projects.length ? projects.map(project => <TableRow key={project.projectId}><TableCell className="font-medium">{project.projectName}</TableCell><TableCell className="text-right tabular-nums">{formatMoney(project.plannedRevenueCents)}</TableCell><TableCell className="text-right tabular-nums">{formatMoney(project.actualRevenueCents)}</TableCell><TableCell className="text-right tabular-nums">{formatMoney(project.employeeCostCents)}</TableCell><TableCell className="text-right tabular-nums">{formatMoney(project.subcontractorCostCents)}</TableCell><TableCell className="text-right tabular-nums">{formatMoney(project.otherCostCents)}</TableCell><TableCell className={`text-right font-semibold tabular-nums ${project.resultCents >= 0 ? 'text-emerald-600' : 'text-rose-600'}`}>{formatMoney(project.resultCents)}</TableCell><TableCell className="text-right tabular-nums">{project.marginPercent === null ? '—' : `${project.marginPercent.toLocaleString('de-DE')} %`}</TableCell></TableRow>) : <TableRow><TableCell colSpan={8} className="h-32 text-center text-slate-500">Keine Projekte gefunden.</TableCell></TableRow>}</TableBody></Table></div></CardContent></Card>
}
